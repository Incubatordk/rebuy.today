---
import AppStoreButtons from './AppStoreButtons.astro';
---
<section class="hero" id="hero">
  <div class="container hero-inner">
    <div class="hero-content">
      <div class="hero-badge fade-in">
        <span class="hero-badge-dot"></span>
        Ny app til børnefamilier i Danmark
      </div>

      <h1 class="hero-title fade-in fade-in-delay-1">
        Køb og sælg<br />
        børneting<br />
        <span class="hero-title-accent">i dit nærområde</span>
      </h1>

      <p class="hero-subtitle fade-in fade-in-delay-2">
        Rebuy forbinder forældre lokalt, så børnetøj, legetøj og udstyr
        får et nyt liv — nemt, trygt og bæredygtigt.
      </p>

      <div class="hero-actions fade-in fade-in-delay-3">
        <AppStoreButtons />
      </div>

      <div class="hero-social-proof fade-in fade-in-delay-4">
        <div class="hero-avatars" aria-hidden="true">
          <div class="avatar" style="background: #E8D5E0;">M</div>
          <div class="avatar" style="background: #D5E0E8;">S</div>
          <div class="avatar" style="background: #E0E8D5;">L</div>
          <div class="avatar" style="background: #E8E0D5;">A</div>
        </div>
        <div class="hero-social-text">
          <div class="hero-stars" aria-label="5 ud af 5 stjerner">
            <svg width="14" height="14" viewBox="0 0 24 24" fill="#F5A623"><path d="M12 2l3.09 6.26L22 9.27l-5 4.87 1.18 6.88L12 17.77l-6.18 3.25L7 14.14 2 9.27l6.91-1.01L12 2z"/></svg>
            <svg width="14" height="14" viewBox="0 0 24 24" fill="#F5A623"><path d="M12 2l3.09 6.26L22 9.27l-5 4.87 1.18 6.88L12 17.77l-6.18 3.25L7 14.14 2 9.27l6.91-1.01L12 2z"/></svg>
            <svg width="14" height="14" viewBox="0 0 24 24" fill="#F5A623"><path d="M12 2l3.09 6.26L22 9.27l-5 4.87 1.18 6.88L12 17.77l-6.18 3.25L7 14.14 2 9.27l6.91-1.01L12 2z"/></svg>
            <svg width="14" height="14" viewBox="0 0 24 24" fill="#F5A623"><path d="M12 2l3.09 6.26L22 9.27l-5 4.87 1.18 6.88L12 17.77l-6.18 3.25L7 14.14 2 9.27l6.91-1.01L12 2z"/></svg>
            <svg width="14" height="14" viewBox="0 0 24 24" fill="#F5A623"><path d="M12 2l3.09 6.26L22 9.27l-5 4.87 1.18 6.88L12 17.77l-6.18 3.25L7 14.14 2 9.27l6.91-1.01L12 2z"/></svg>
          </div>
          <span>Elsket af forældre i hele Danmark</span>
        </div>
      </div>
    </div>

    <div class="hero-visual fade-in fade-in-delay-2" aria-hidden="true">
      <div class="hero-visual-glow"></div>
      <div class="hero-logo-wrapper">
        <Fragment set:html={await Astro.slots.render('logo')} />
      </div>
      <!-- Floating category pills for visual interest -->
      <div class="hero-pill pill-1">Børnetøj</div>
      <div class="hero-pill pill-2">Legetøj</div>
      <div class="hero-pill pill-3">Barnevogne</div>
      <div class="hero-pill pill-4">Bøger</div>
    </div>
  </div>
</section>

<style>
  .hero {
    padding: var(--space-20) 0 var(--space-24);
    position: relative;
    overflow: hidden;
  }

  .hero::before {
    content: '';
    position: absolute;
    top: -50%;
    right: -20%;
    width: 700px;
    height: 700px;
    background: radial-gradient(circle, var(--color-brand-pink-light) 0%, transparent 70%);
    border-radius: 50%;
    pointer-events: none;
    z-index: 0;
  }

  .hero-inner {
    display: flex;
    align-items: center;
    gap: var(--space-16);
    position: relative;
    z-index: 1;
  }

  .hero-content {
    flex: 1;
    max-width: 560px;
  }

  .hero-badge {
    display: inline-flex;
    align-items: center;
    gap: var(--space-2);
    background: var(--color-accent-light);
    color: var(--color-accent-dark);
    padding: var(--space-2) var(--space-4);
    border-radius: var(--radius-full);
    font-size: var(--font-size-sm);
    font-weight: var(--font-weight-medium);
    margin-bottom: var(--space-8);
  }

  .hero-badge-dot {
    width: 8px;
    height: 8px;
    background: var(--color-accent);
    border-radius: 50%;
    animation: pulse 2s ease-in-out infinite;
  }

  @keyframes pulse {
    0%, 100% { opacity: 1; }
    50% { opacity: 0.4; }
  }

  .hero-title {
    font-size: clamp(2.5rem, 6vw, var(--font-size-6xl));
    font-weight: var(--font-weight-bold);
    line-height: var(--line-height-tight);
    margin-bottom: var(--space-6);
    color: var(--color-text-primary);
    letter-spacing: -0.03em;
  }

  .hero-title-accent {
    color: var(--color-accent);
    font-style: italic;
  }

  .hero-subtitle {
    font-size: var(--font-size-xl);
    color: var(--color-text-secondary);
    line-height: var(--line-height-relaxed);
    margin-bottom: var(--space-10);
    max-width: 480px;
  }

  .hero-actions {
    margin-bottom: var(--space-10);
  }

  /* Social proof */
  .hero-social-proof {
    display: flex;
    align-items: center;
    gap: var(--space-4);
  }

  .hero-avatars {
    display: flex;
  }

  .avatar {
    width: 36px;
    height: 36px;
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: var(--font-size-xs);
    font-weight: var(--font-weight-semibold);
    color: var(--color-text-secondary);
    border: 2px solid var(--color-bg-primary);
    margin-right: -10px;
  }

  .avatar:last-child {
    margin-right: 0;
  }

  .hero-social-text {
    display: flex;
    flex-direction: column;
    gap: 2px;
  }

  .hero-stars {
    display: flex;
    gap: 1px;
  }

  .hero-social-text span {
    font-size: var(--font-size-sm);
    color: var(--color-text-muted);
  }

  /* Visual / illustration side */
  .hero-visual {
    flex: 1;
    display: flex;
    justify-content: center;
    align-items: center;
    position: relative;
  }

  .hero-visual-glow {
    position: absolute;
    width: 340px;
    height: 340px;
    background: radial-gradient(circle, var(--color-brand-pink) 0%, transparent 70%);
    opacity: 0.15;
    border-radius: 50%;
    z-index: 0;
    animation: breathe 4s ease-in-out infinite;
  }

  @keyframes breathe {
    0%, 100% { transform: scale(1); }
    50% { transform: scale(1.08); }
  }

  .hero-logo-wrapper {
    width: 100%;
    max-width: 380px;
    position: relative;
    z-index: 1;
  }

  .hero-logo-wrapper :global(svg) {
    width: 100%;
    height: auto;
  }

  /* Floating pills */
  .hero-pill {
    position: absolute;
    background: var(--color-bg-card);
    padding: var(--space-2) var(--space-4);
    border-radius: var(--radius-full);
    font-size: var(--font-size-xs);
    font-weight: var(--font-weight-medium);
    color: var(--color-text-secondary);
    box-shadow: var(--shadow-md);
    z-index: 2;
    animation: float 6s ease-in-out infinite;
  }

  .pill-1 { top: 5%; right: 5%; animation-delay: 0s; }
  .pill-2 { top: 60%; right: -5%; animation-delay: 1.5s; }
  .pill-3 { bottom: 10%; left: 0%; animation-delay: 3s; }
  .pill-4 { top: 25%; left: -10%; animation-delay: 4.5s; }

  @keyframes float {
    0%, 100% { transform: translateY(0); }
    50% { transform: translateY(-10px); }
  }

  @media (max-width: 900px) {
    .hero {
      padding: var(--space-12) 0 var(--space-16);
    }

    .hero-inner {
      flex-direction: column-reverse;
      text-align: center;
      gap: var(--space-10);
    }

    .hero-content {
      max-width: 100%;
      display: flex;
      flex-direction: column;
      align-items: center;
    }

    .hero-subtitle {
      max-width: 100%;
    }

    .hero-logo-wrapper {
      max-width: 280px;
    }

    .hero-content :global(.store-buttons) {
      justify-content: center;
    }

    .hero-social-proof {
      justify-content: center;
    }

    .hero-pill {
      display: none;
    }
  }
</style>
